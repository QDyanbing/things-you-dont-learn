import ReactDOM from 'react-dom/client';
import App from './App';

/**
 * Mounts the upload demo into the Vite HTML root element.
 */
ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
